# Deprecated — kept so existing `ansible-playbook -i inventory.ini ...`
# invocations don't fail with "no such file". The new inventory lives
# at `inventory/hosts.yml` (group + host_vars structure). Use deploy.sh
# instead, which points ansible-playbook at the new inventory.
#
# Migration:
#   OLD:  ansible-playbook -i inventory.ini --limit nairobi -e boot_tenant=ke.nairobi playbook-deploy.yml
#   NEW:  ./deploy.sh nairobi
#
# This file will be removed once all known call sites are migrated.
