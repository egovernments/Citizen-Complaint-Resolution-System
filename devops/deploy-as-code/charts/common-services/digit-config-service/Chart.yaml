apiVersion: v2
name: digit-config-service
description: Schema-validated, tenant-scoped configuration store for notification template bindings, provider details, and channel toggles
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
- name: common
  version: 0.0.5
  repository: file://../../common
