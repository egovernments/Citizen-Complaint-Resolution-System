{{- if .Values.distUrl }}
apiVersion: v1
kind: Service
metadata:
  name: digit-ui-v2
  namespace: {{ .Values.namespace | default "egov" }}
  labels:
    app: digit-ui-v2
spec:
  selector:
    app: digit-ui-v2
  ports:
    - port: 80
      targetPort: 80
{{- end }}
