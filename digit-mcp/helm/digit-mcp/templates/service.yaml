apiVersion: v1
kind: Service
metadata:
  name: {{ include "digit-mcp.fullname" . }}
  labels:
    {{- include "digit-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digit-mcp.selectorLabels" . | nindent 4 }}
