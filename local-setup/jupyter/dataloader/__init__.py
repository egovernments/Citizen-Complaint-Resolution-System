"""DIGIT/PGR tenant bootstrap dataloader.

Loaded by the Jupyter notebook (`from dataloader.crs_loader import CRSLoader`)
and by the CI test scripts in /opt/digit/ci-tests/scripts/ (which insert
this directory into sys.path and import `crs_loader` directly).
"""
