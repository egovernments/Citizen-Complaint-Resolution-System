{
  "name": "digit-smoke-tests",
  "version": "1.0.0",
  "description": "Smoke tests for DIGIT core services with schema validation",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:verbose": "jest --runInBand --verbose",
    "test:e2e": "npx playwright test --config=e2e/playwright.config.ts",
    "test:e2e:headed": "npx playwright test --config=e2e/playwright.config.ts --headed"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/jest": "^29.5.11",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.6",
    "@types/pg": "^8.10.9",
    "jest": "^29.7.0",
    "js-yaml": "^4.3.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
