node_modules/
test-results/
playwright-report/
.env
/tmp/
*.png
# …but test fixtures must be tracked (the broad *.png above was swallowing them,
# leaving avatar-upload specs to fail with ENOENT on a fresh checkout).
!tests/fixtures/*.png

# Generated by auth.setup.ts — contains session token, never commit.
auth.json
# Generated by api.setup.ts — contains token-injected session, never commit.
auth-api.json
.worktrees/
report.json
catalog.json
history.json
.git-sha
lifecycle-fixtures.json
test-results/
citizen-fixture.json
# Generated by profile.setup.ts — a snapshot of whatever deployment the last run
# targeted, so committing it would silently pin one stack's shape onto another.
deployment-profile.json
