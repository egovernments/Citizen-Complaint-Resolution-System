{
  "name": "baileys-send-service",
  "version": "0.1.0",
  "description": "Minimal HTTP wrapper around a single Baileys WhatsApp socket. Exposes POST /send {to,text} so novu-bridge (BaileysProviderStrategy) can deliver free-form WhatsApp messages for PGR config-driven notifications. Auth state is persisted to a mounted volume; pairing is done once via the QR endpoint.",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "baileys": "^7.0.0-rc.9",
    "express": "^4.19.2",
    "pino": "^9.5.0",
    "qrcode": "^1.5.4"
  }
}
