{
  "name": "digit-workflow-designer",
  "version": "0.1.0",
  "description": "DIGIT workflow designer (forked from workflow.egov.theflywheel.in) — vanilla React + dagre + esbuild + postMessage bridge.",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "dagre": "^0.8.5"
  }
}
